# Create railpack.json
@'
{
  "providers": {
    "php": {
      "version": "8.4"
    }
  }
}
'@ | Out-File -FilePath railpack.json -Encoding utf8

# Create .php-version
@'
8.4
'@ | Out-File -FilePath .php-version -Encoding utf8
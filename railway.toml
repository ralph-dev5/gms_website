[build]
builder = "dockerfile"

[deploy]
startCommand = "php artisan config:clear && php artisan cache:clear file && php artisan view:clear && php artisan route:clear && php artisan serve --host=0.0.0.0 --port=${PORT:-8000}"

